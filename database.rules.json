{
  "rules": {
    ".read": false,
    ".write": false,

    "overrides": {
      ".read": true,
      ".write": "auth != null && auth.token.email == 'admin@vippods.app'"
    },

    "pricing": {
      ".read": true,
      ".write": "auth != null && auth.token.email == 'admin@vippods.app'"
    },

    "perfumes": {
      ".read": true,
      ".write": "auth != null && auth.token.email == 'admin@vippods.app'"
    },

    "settings": {
      ".read": true,
      ".write": "auth != null && auth.token.email == 'admin@vippods.app'"
    }
  }
}
